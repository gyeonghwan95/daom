# 08 — Content plan (post-surgery)

## P0 — 배포 직후
1. IndexNow 2 URL (`16-indexnow-targets.txt`)
2. Naver 웹마스터 URL 재수집 요청 (`17-naver-submit-checklist.md`)
3. Build + `inheritance-seo-snapshot.mjs --phase=after` → protected URL hash

## P1 — 2주 관측
- 「부산 상속포기 법무사」: 노출 URL이 `/부산상속포기`로 전환되는지
- 「부산 상속 법무사」: 허브 rank·스니펫에서 포기 키워드 비중 감소 여부
- 「부산 상속전문 법무사」: hub FAQ 스니펫 반영 여부

## P2 — inbound (승인 후만, `15-recommended-inbound-links.md`)
- `/부산법무사` 상속 블록 → `/부산상속포기` 앵커 분리
- `/부산상속등기` → 포기 vs 등기 분기 CTA

## 금지
- `/부산상속전문법무사` redirect/delete
- hub title에「포기」only 강조 회귀
