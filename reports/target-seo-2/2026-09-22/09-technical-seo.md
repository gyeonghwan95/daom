# 09 — Technical SEO (targets)

All three targets: HTTP 200 (static export), indexable, self-canonical, unique title/H1, in sitemap (pre-existing), IndexNow submitted.

No meta keywords added. No shared layout/nav/footer changes.
